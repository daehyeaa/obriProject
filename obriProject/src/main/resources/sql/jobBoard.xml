<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="music.dao.jobBoardDao">

	<insert id="jobInsert" parameterType="jobboard">
	insert into jobBoard (jobNo,userId,jobCd,jobFin,jobSpace,jobAddress,
	jobEndDate,jobConcert,jobSub,jobText,jobRcount,jobDate,jobDelYn)
	values (job_no_seq.nextval,#{userId},#{jobCd},'N',#{jobSpace},#{jobAddress},
	#{jobEndDate},#{jobConcert},#{jobSub},#{jobText},0,sysdate,
	'N');
	</insert>
	
	<select id="jobList" parameterType="jobboard"
		resultType="jobboard">
		select * from (select a.*,rowNum rn from (
		select * from jobBoard
		<where>
			<if test="keyword != null and search!='subcon'">
				${search} like '%'||#{keyword}||'%'
			</if>
			<if test="keyword != null and search=='subcon'">
				jobSub like '%'||#{keyword}||'%' or
				jobText like '%'||#{keyword}||'%'
			</if>
		</where>
		order by jobNo desc) a )
		where rn between #{startRow} and #{endRow}
	</select>
	
	<select id="getJobTotal" parameterType="jobboard" resultType="int">
		select count(*) from jobBoard
		<where>
			<if test="keyword != null and search !='subcon'">
				${search} like '%'||#{keyword}||'%'
			</if>
			<if test="keyword != null and search=='subcon'">
				jobSub like '%'||#{keyword}||'%' or
				jobText like '%'||#{keyword}||'%'
			</if>
		</where>
	</select>
	
	<update id="update" parameterType="jobboard">
		update jobBoard set jobSpace=#{jobSpace},jobCd=#{jobCd},
		jobSub=#{jobSub},jobText=#{jobText} where jobNo=#{jobNo}
	</update>
	
	<update id="updateCount" parameterType="int">
		update jobBoard set jobRcount = jobRcount+1 where jobNo=#{jobNo}
	</update>

	<select id="getJobBoard" parameterType="int" resultType="jobboard">
		select * from jobBoard where jobNo=#{jobNo}
	</select>

	<update id="delete" parameterType="int">
		update jobBoard set jobDelYn = 'Y' where jobNo=#{jobNo}
	</update>

	<select id="select" parameterType="int" resultType="jobboard">
		select * from jobBoard where jobNo=#{jobNo}
	</select>

</mapper>